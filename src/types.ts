export interface CardProps {
        admin_id:number,
        completed:boolean,
        description: string,
        header_img: string,
        id: number,
        name: string,
        place: string,
        registration_fee: number,
        tagline: string,
        start_date: Date;
        end_date: Date,
        address: string,
}

export interface response{
        
}